fint:
  org-id: ''
  application-id: fint-kontroll-user-factory
  cache:
    defaultCacheEntryTimeToLiveMillis: 604800000
    defaultCacheHeapSize: 1000000
  kontroll:
    user:
      publishing:
        initial-delay: 200000
        initial-delay-elev: 300000
        fixed-delay: 3600000
      days-before-start-employee: 10
      days-before-start-student: 5
novari:
  kafka:
    topic:
      org-id: ${fint.org-id}
      domain-context: kontroll
    application-id: ${fint.application-id}
    default-replicas: 1

spring:
  kafka:
    consumer:
      group-id: ${fint.application-id}
  profiles:
    include:
      - observability
logging:
  level:
    root: INFO
    org.apache.kafka: INFO
    no.fintlabs.cache.ehcache.FintEhCacheEventLogger: WARN
